
// <Link id={ele.id} to={'/' + ele.id}>{ele.title}</Link>
//            <Route path='/:id' component={Weather}/>

/*
{this.props.data.map((ele, i) => {
    if (ele.id === this.props.match.params.id) {
      return (
        <div key={i} >
        <h4>{ele.title}</h4>
        <p dangerouslySetInnerHTML={{__html: ele.description}} />
        </div>
      );
    }
  }
)}
/*
